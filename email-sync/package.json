{
  "name": "email-sync",
  "version": "1.0.0",
  "description": "Pulls job-application emails from Gmail, classifies them with free rule-based + LLM-fallback logic, and upserts them into the job-tracker jobs table.",
  "main": "src/run.js",
  "scripts": {
    "test": "node --test test/*.test.js",
    "start": "node src/run.js",
    "get-token": "node src/getRefreshToken.js"
  },
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "dotenv": "^17.3.1",
    "googleapis": "^144.0.0"
  }
}
